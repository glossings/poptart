{
  "name": "@poptart/web-engine",
  "license": "AGPL-3.0-only",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "The browser engine adapter: a Web Audio implementation of the engine interface pattern-core's scheduler drives, plus the catalog of built-in synths and effects that replaces plugin hosting on the web.",
  "main": "src/index.mjs",
  "scripts": {
    "test": "node --test",
    "build": "node build/bundle-worklets.mjs && node build/render-packs.mjs"
  }
}
